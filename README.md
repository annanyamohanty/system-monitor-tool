# 🖥️ System Monitor Tool (C++)

**Author:** Annanya Mohanty  
**Environment:** Ubuntu (WSL on Windows)  
**Description:** A simple system monitor built in C++ that displays real-time CPU usage, memory usage, and top processes (similar to the Linux `top` command).

## 🔧 How to Build
```bash
g++ system_monitor.cpp -o system_monitor
